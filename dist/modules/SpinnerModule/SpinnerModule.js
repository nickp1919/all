import React from 'react';
import { Spinner } from '@pulse/ui/components/Spinner';
const SpinnerModule = (props) => {
    return React.createElement(Spinner, { ...props });
};
export default SpinnerModule;
