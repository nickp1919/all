import { ReactNode } from 'react';
import { Size } from '@pulse/ui/components/Title/types';
export declare type ActionBlockProps = {
    size: Size;
    children?: ReactNode;
    onClick?: () => void;
};
